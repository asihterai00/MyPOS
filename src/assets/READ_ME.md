# Assets Directory
This directory is intended for static assets such as images, icons, and logos used within the PRISM POS application.
Current implementation primarily uses `lucide-react` for iconography.
